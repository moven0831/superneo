/target
**/*.rs.bk
*.profraw
.DS_Store

# Reference material. The paper PDFs stay local and are never vendored; the corpus
# README is tracked, and every reference implementation is linked as a lightweight
# git submodule so its tree is referenced rather than copied into this repo.
/context/papers
